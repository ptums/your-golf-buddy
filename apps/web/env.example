# Cloud Sync Configuration
# Copy this file to .env.local and update with your actual sync endpoint

# Cloud sync endpoint (replace with your actual Neon/cloud database endpoint)
NEXT_PUBLIC_SYNC_ENDPOINT=https://your-neon-db-endpoint.com/sync

# Optional: Sync frequency override (in milliseconds, default is 24 hours)
# NEXT_PUBLIC_SYNC_INTERVAL_MS=86400000
